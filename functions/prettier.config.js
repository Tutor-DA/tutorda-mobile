module.exports = {
  semi: true, // Always add semicolons
  singleQuote: false, // Use double quotes (matches your ESLint "quotes" rule)
  printWidth: 80, // Max line length
  tabWidth: 2, // 2 spaces per indent level
  trailingComma: "es5", // Add trailing commas where valid in ES5 (objects, arrays, etc.)
  bracketSpacing: true, // Print spaces between brackets: { foo: bar }
  arrowParens: "always", // Always include parens around arrow function params
};
